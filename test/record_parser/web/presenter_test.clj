(ns record-parser.web.presenter-test
  (:require
    [record-parser.web.presenter :as presenter]
    [clojure.test :refer :all]))

;;(deftest web-presenter-test
;;  (testing "it converts the date to a string representation for json encoding"
;;    (it (= "1/1/2019" (presenter/present {:birthdate (string->date "2019-01-01")})))))
